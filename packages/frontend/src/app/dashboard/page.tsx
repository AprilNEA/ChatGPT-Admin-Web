'use client';

import { Box, Container } from '@radix-ui/themes';

export default function DashboardIndex() {
  return (
    <Container
      style={{
        height: '100%',
        margin: '10%',
      }}
    ></Container>
  );
}
